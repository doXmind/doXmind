# Generated by tauri-build at compile time.
/gen/

# Cargo build artifacts.
/target/

# Bundled sidecar binaries — produced by scripts/build-sidecar.mjs.
# Keep the directory itself (with .gitkeep) so externalBin lookup doesn't fail.
/binaries/*
!/binaries/.gitkeep
