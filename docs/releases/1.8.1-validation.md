# doXmind 1.8.1 candidate validation

Validation date: 2026-07-22

This record binds the immutable `v1.8.1` draft in `doXmind/releases` to the
Electron-only, Markdown-native source and the exact six downloaded assets. No
candidate asset was replaced after the draft was created.

## Source and build environment

- Source commit: `dae8feca4dfeb37c03d9900696e38e3db67662f1`
- Source tree: `f153da205bbaa15f3d3e9794664a9318f60b52fd`
- Release-preparation PR: `doXmind/doXmind-app#195`
- PR CI: all Backend, Frontend, Electron native/smoke, production build, and
  Chromium E2E jobs passed. Its tested tree equals the source tree above.
- Build host: macOS 26.5 (`25F71`), Apple M4 Max, arm64
- Toolchain: Node `26.0.0`, npm `11.12.1`, electron-builder `26.15.3`
- Signing identity: Developer ID Application `wangzhang wu (46KF5Z549N)`
- Apple notarization submission: `ec3e8219-442d-42e7-83f3-4753bcccccff`
  (`Accepted`)

## Immutable candidate assets

The files below were downloaded again from the Draft and verified through
`scripts/verify-release-artifacts.mjs` against
[`1.8.1-artifacts.sha256`](1.8.1-artifacts.sha256).

| Asset                                  | Bytes       | SHA-256                                                            |
| -------------------------------------- | ----------- | ------------------------------------------------------------------ |
| `doXmind-1.8.1-arm64-mac.zip`          | 119,515,309 | `69929b3e6e04285150f5dd46319741603243afd889b733a54435df19110b1643` |
| `doXmind-1.8.1-arm64-mac.zip.blockmap` | 126,563     | `e1ea2f129197d0bb6afef121d531fbafd76cf0bc918c4dce3aa272a99ced644f` |
| `doXmind-1.8.1-arm64.dmg`              | 119,986,596 | `c4b7622266056485390874334f1598b15a6760740504672fbecb8a72cab7e9f0` |
| `doXmind-1.8.1-arm64.dmg.blockmap`     | 127,322     | `aff5eb1bd8155f691c7e8f1bdff425d58af2425505efb5f410b5c5608c88a954` |
| `doXmind-mac-arm64.dmg`                | 119,986,596 | `c4b7622266056485390874334f1598b15a6760740504672fbecb8a72cab7e9f0` |
| `latest-mac.yml`                       | 505         | `8443d65a935987c993ca43e6989dc4f4b0fd249601d114b6b7943b0ccfb9edc2` |

The fixed-name DMG is byte-identical to the versioned DMG. `latest-mac.yml`
names version `1.8.1`, points to the versioned ZIP and DMG, and its byte counts,
SHA-512 values, and top-level update ZIP match the downloaded files.

## Package integrity and distribution

- `hdiutil verify` accepted the downloaded DMG; `unzip -t` accepted the
  downloaded update ZIP.
- The Apps extracted from the update ZIP and mounted from the DMG both passed
  `codesign --verify --deep --strict` and Apple `syspolicy_check distribution`.
- Both Apps contain a valid stapled notarization ticket, bundle identifier
  `com.doxmind.desktop`, versions `1.8.1` / `1.8.1`, and an arm64 executable.
- Both identify Team `46KF5Z549N` and CDHash
  `f330f5d2d72521736d2209ce81bc9eedf35cbc8e`.
- The downloaded update App's ASAR matches the locally scanned candidate at
  SHA-256 `8c7b9370cd74c0afbb760310ea37c40cf1ad6737f1d764a4fee0d195b91239a7`.
  It contains 202 files and no Python/FastAPI server, Tauri/Rust runtime,
  TipTap, ProseMirror, or packaged workspace sidecar service.

The host's legacy `spctl` command returned `Too many open files` for both this
App and Apple's Safari during validation. It was not counted as evidence.
Apple's current distribution check, notarization history, stapled ticket, and
deep code-signature verification all passed independently.

## Downloaded-candidate GUI gate

The packaged Electron acceptance suite launched the App extracted from the
downloaded update ZIP with isolated user data and a temporary workspace. All
19 checks passed with no renderer `pageerror` or `console.error`:

- real preload IPC, sandbox preferences, native menus, windows, and Block
  duplicate/move/delete/undo;
- Markdown autosave/reopen, Collections, computed fields, embeds, and safe
  pasted/dropped images;
- Page recovery and read-only Attachment behavior;
- Settings and light/dark themes; and
- real printer-independent Page PDF output plus Save-dialog cancellation.

The generated PDF was a valid one-page, 38,648-byte file containing the Page
title and body without application chrome. The Markdown source, complete legacy
artifact family, and workspace entries remained byte-identical. Cancellation
wrote no output and also preserved every source and recovery artifact.

## Publication boundary

The release request authorizes the immediate window after the provenance PR
and its CI are green. Publication must only flip this verified Draft to
public/latest. It must not rebuild or upload assets. Immediately afterward,
verify the public `latest` release, fixed DMG URL, `1.7.8` update response, and
the `1.8.1` no-update response against these same bytes.
