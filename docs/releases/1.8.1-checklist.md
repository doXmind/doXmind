# doXmind 1.8.1 publication checklist

Status: READY TO PUBLISH

Change the status to exactly `READY TO PUBLISH` only after every unchecked item
is complete. Publication verifies and exposes the existing immutable draft; it
must never rebuild or replace candidate bytes.

## Build provenance

| Item             | Value                                                                                                                      |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------- |
| App version      | `1.8.1`                                                                                                                    |
| Source commit    | `dae8feca4dfeb37c03d9900696e38e3db67662f1` (tree `f153da205bbaa15f3d3e9794664a9318f60b52fd`)                               |
| Signed artifacts | Six assets pinned by [SHA-256 manifest](1.8.1-artifacts.sha256) and the [candidate validation record](1.8.1-validation.md) |

## Gate

- [x] `package.json` and `package-lock.json` agree on `1.8.1`.
- [x] The Electron-only Markdown workspace is merged and its frontend, backend,
      native Electron, smoke, and Chromium E2E checks pass.
- [x] Public notes describe the current Markdown-only Page and read-only
      Attachment boundary without promising the retired editor/recovery stack.
- [x] A new `v1.8.1` draft contains exactly six signed and notarized candidate
      assets built from the recorded `doXmind-app` source commit.
- [x] The committed SHA-256 manifest, GitHub asset digests, stable DMG alias,
      and `latest-mac.yml` version, names, sizes, and SHA-512 values all match.
- [x] The downloaded DMG and update ZIP pass integrity, Developer ID,
      Gatekeeper, notarization ticket, bundle version, architecture, and
      packaged-runtime boundary checks.
- [x] Packaged Electron GUI acceptance passes from the downloaded candidate,
      including native Block editing and printer-independent PDF Save/cancel.
- [x] Page and Attachment recovery checks leave Markdown, source Attachments,
      `.doxmind`, `.bak`, `.lock`, and `.corrupt-*` artifacts byte-identical.
- [x] Build provenance above records the exact source commit and six verified
      signed artifact names and hashes.
- [x] The immediate release window is confirmed and the fixed latest-download
      alias plus automatic-update metadata are ready to expose the same bytes.

Do not publish or modify the superseded `v1.8.0` draft.
