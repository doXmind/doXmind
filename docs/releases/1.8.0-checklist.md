# doXmind 1.8.0 publication checklist

Status: SUPERSEDED — DO NOT PUBLISH

The immutable `v1.8.0` draft was built before the Electron-only,
Markdown-native workspace landed on `main`. Preserve that draft for audit; do
not publish, replace, or reuse its assets. The corrected release is `1.8.1`.

The remaining entries are historical validation notes. They must not be
advanced to `READY TO PUBLISH`.

## Build provenance

| Item             | Value                      |
| ---------------- | -------------------------- |
| App version      | `1.8.0`                    |
| Source commit    | Pending release validation |
| Signed artifacts | Pending release validation |

## Gate

- [x] Version manifests agree on `1.8.0`.
- [x] Automated frontend, backend, Rust, GUI, and Electron smoke checks pass.
- [x] Current overview and Page-editor screenshots replace the retired product
      surface; unreferenced PDF, spreadsheet, and DatabaseBlock media are gone.
- [ ] A signed and notarized candidate installs and passes the packaged-app gate
      in [GUI Preflight](../GUI_PREFLIGHT.md#release-gate).
- [x] One exact older fallback artifact is validated against isolated real
      legacy PDF and workbook evidence; the public notes contain its version,
      download URL, SHA-256, platform, and validation record.
- [ ] Supported recovery and rejected/unknown cases leave the source, main
      sidecar, every `.bak`, `.lock`, and `.corrupt-*` byte-for-byte unchanged.
- [ ] Build provenance above contains the exact source commit and verified
      signed artifact names and hashes.
- [x] Public notes contain no pending values or internal checklist language.
- [ ] The draft contains exactly the six manifest-pinned assets; the stable
      website DMG is byte-identical to the versioned DMG and `latest-mac.yml`
      matches the DMG and update ZIP.
- [ ] The website boundary/version update is approved and its production
      deployment owner and release window are confirmed.

Do not replace an existing release asset in place or publish a candidate as
`latest`.
