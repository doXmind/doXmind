"""The `doxmind` command-line interface (ADR 0010).

A standalone document toolkit. It imports the `core` facade directly and
operates on the filesystem, so it needs no running desktop app or HTTP sidecar.
"""
