# Optional browser-development command mirror. Packaged desktop builds do not
# load this file or start Python/FastAPI. There are no API credentials.
HOST=127.0.0.1
PORT=8000
DEBUG=true

# Override the data directory if you don't want ~/.doxmind
# DATA_DIR=/path/to/your/doxmind-data
