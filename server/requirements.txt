# doXmind Mini — local desktop edition

# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.7.0

# Database — local SQLite
sqlalchemy==2.0.36
aiosqlite==0.20.0
greenlet==3.1.1

# HTTP
python-multipart==0.0.19
httpx==0.28.1

# Export
markdown==3.7
markdownify==1.2.0
fpdf2==2.8.5
python-docx==1.1.2
beautifulsoup4==4.12.3

# Import (PDF/DOCX → markdown locally via markitdown)
# Intentionally unpinned — always pull the latest markitdown for new format
# support and bug fixes. Run `pip install -U markitdown[all]` to refresh.
markitdown[all]
pypdf>=4.0.0

# Spreadsheet parsing — required transitively by markitdown's xlsx converter.
pandas==2.2.3
numpy==2.2.1
openpyxl==3.1.5

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
